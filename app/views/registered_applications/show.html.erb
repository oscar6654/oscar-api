<div class="row">
  <div class="col-md-8">
    <h1><%= @registered_application.name %></h1>
    <h3><%= link_to @registered_application.URL, @registered_application.URL, :target => "_blank" %></h3>
  <div>
  <div class='row'>
    <div class='col-md-8'>
      <table class="table table-striped">
        <tbody>
          <thead>
            <td class="text-uppercase">Event Name</td>
            <td class="text-uppercase">Event Count</td>
          </thead>

          <% @events.each do |name, events| %>
            <tr>
            <td><%= name %></td>
            <% count = 0 %>
              <% events.each{ |event| count += 1 } %>
            <td><%= count %></td>
          </tr>
          <% end %>

        </tbody>
      </table>
    </div>
    <div class='row'>
      <div class='col-md-8'>
        <div class="bloc_buttons">
          <%= link_to " Edit", edit_registered_application_path, class: 'btn btn-success glyphicon glyphicon-edit' %>
          <%= link_to " Delete", registered_application_path, method: :delete, data: { confirm: 'Are you sure you want to delete this website?'}, id: 'delete-site', class: 'btn btn-danger glyphicon glyphicon-remove' %>
        </div>
          <%= link_to "Back to Website List", root_path, class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
</div>
